// import map from './scripts/map.js';


// import {Histogram} from "d3/histogram"
const values = [1.21, 1.02, 0.61, 0.99, 1.53]

function main() {
  d3.select("p").style("color", "firebrick");
}

// console.log(fetch('https://population.un.org/dataportalapi/api/v1'))
// will add an object as the second argument for the selector we want(this is all indicators)
// returns a promise, therefore can use async await or .then or .catch
  // .then(res => console.log(res))
  // returning an error "cannot read properties of undefined (reading 'then')"

  d3.select


// document.addEventListener('DOMContentLoaded', () => {
//   // console.log("hello world")
//   const root = document.
//   querySelector('#root')

//   new Example(root);
// })

// document.addEventListener('DOMContentLoaded', async () => {
//   const mapDiv = document.getElementById('map');
//   mapDiv.style.width = '50%';

//   const res = await fetch(`https://cdn.jsdelivr.net/npm/world-atlas@2.0.2/countries-50m.json`)
//   const mapJson = await res.json()
//   const foo = map(mapJson)
//   debugger

//   mapDiv.appendChild(foo);
// });

